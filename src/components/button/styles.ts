import { CSSProperties } from "react";

export const styles = {
    container: {
        borderRadius   : 10,
        textAlign      : "center",
        border         : "2px solid #111",
        boxShadow      : "0 6px 18px rgba(0,0,0,0.15)",
    } as CSSProperties,
    
    link: {
        display        : "block",
        padding        : "10px",
        color          : "#111",
        textDecoration : "none",
        fontWeight     : "bold",
        letterSpacing  : "0.2px",
    } as CSSProperties,
};
